# Aria Voice Agent — Environment Variables
# Copy this file to .env and fill in your actual API keys.
# NEVER commit .env to git (it's in .gitignore).

# Required for the voice agent (SmallWebRTC — no Daily key needed)
DEEPGRAM_API_KEY=your_deepgram_key
OPENAI_API_KEY=your_openai_key
CARTESIA_API_KEY=your_cartesia_key

# Optional — latency tuning (see ARCHITECTURE.md § Latency vs Quality Tradeoffs)
# OPENAI_MODEL=gpt-4o                      # default: gpt-4o-mini (faster), gpt-4o (smarter)
# DEEPGRAM_UTTERANCE_END_MS=1000           # default: safer for slow speakers
# DEEPGRAM_ENDPOINTING=250                 # default
# TTS_LOW_LATENCY=false                    # default: SENTENCE mode (more natural)

# ——— BALANCED / QUALITY (~600–700ms) ———
# DEEPGRAM_UTTERANCE_END_MS=600
# DEEPGRAM_ENDPOINTING=200

# ——— LOW LATENCY — aggressive endpointing for responsive voice ———
# OPENAI_MODEL=gpt-4o-mini
DEEPGRAM_UTTERANCE_END_MS=300
DEEPGRAM_ENDPOINTING=150
TTS_LOW_LATENCY=true
# Smaller context = faster LLM TTFB (defaults: MAX_CONTEXT_MESSAGES=40, CONTEXT_SUMMARY_THRESHOLD=30)

# ——— DUAL LLM ROUTING — simple → gpt-4o-mini, complex → gpt-4o ———
# ENABLE_DUAL_LLM=true
# OPENAI_MODEL_FAST=gpt-4o-mini
# OPENAI_MODEL_SMART=gpt-4o
# USE_LLM_CLASSIFIER=true   # true: LLM classifier (~50ms), false: keyword matching (0ms)

# ——— BACKCHANNELING (TODO: not implemented) ———
# ENABLE_BACKCHANNELING=true   # "mhm", "okay" during long user turns; see agent/backchanneling.py

# Database (default: SQLite; use Postgres in production)
# DATABASE_URL=sqlite+aiosqlite:///data/aria.db
# DATABASE_URL=postgresql://user:pass@host:5432/aria_db

# Redis (optional — for session state in multi-instance deployments)
# REDIS_URL=redis://localhost:6379/0

# Transport: webrtc | twilio | daily
# TRANSPORT_MODE=webrtc

# Server (default 7860; set PORT if "address already in use", e.g. PORT=7861)
# PORT=7860
# HOST=0.0.0.0

# Context window
# MAX_CONTEXT_MESSAGES=50
# CONTEXT_SUMMARY_THRESHOLD=40

# Optional — for observability / deployment
# LOG_LEVEL=INFO
# LOGFIRE_TOKEN=your_logfire_token
# FLY_API_TOKEN=your_fly_token
